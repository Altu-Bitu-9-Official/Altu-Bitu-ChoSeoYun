#include <iostream>

using namespace std;

/*
바퀴에 같은 글자는 두 번 이상 등장하지 않는다.
바퀴를 돌릴 때 마다, 화살표가 가리키는 글자가 변하는 횟수와 어떤 글자에서 회전을
멈추었는지 종이에 적는다.
*/

int main() {

  int n, k, s;
  string str;

  cin >> n >> k;

  for (int i = 0; i < n; i++) {
    cin >> s >> str;

    

  }

  return 0;
}